<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.wolfcode.mapper.AccountLogMapper">
    <insert id="insert">
        insert into t_account_log (trade_no, pk_value, type, amount, gmt_time, info)
        values (#{tradeNo}, #{pkValue}, #{type}, #{amount}, #{gmtTime}, #{info})
    </insert>

    <select id="selectByPkAndType" resultType="cn.wolfcode.domain.AccountLog">
        select trade_no tradeNo, pk_value pkValue, type, amount, gmt_time gmtTime, info
        from t_account_log
        where pk_value = #{pkValue}
          and type = #{type}
    </select>

</mapper>